#include "myLinal.h"

int main() {
    Matrix result, matrix_, matrix, res2;
    cout.setf(ios::fixed);
    cout.precision(2);
    Matrix m1(matrix.m);
    cin >> matrix_;
    result = matrix + matrix_;
    res2 = matrix - matrix_;
    cout << result << endl;
    cout << res2;
    return 0;
}